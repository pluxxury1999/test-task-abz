import "./Spinner.scss"

const Spinner = ({ width, height }) => {
    return (
        <div className="spinner" style={{"width": `${width}px`, "height": `${height}px`}}>

        </div>
    )
};

export default Spinner;